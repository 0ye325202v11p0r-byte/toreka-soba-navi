# リリース判定（無料版）— 2026-09-13時点

このドキュメントは、ユーザーの最新の要求「ワンピースカード相場アプリを完成させろ」を受けて、Codexからの指示（2026-09-13）に基づき作成した。**Stripe決済（Phase 3）・メール通知の実送信（Phase 4）・AI市場サマリー等、まだ依頼されていない新機能は、この「完成」判定の対象に含めない。** 対象は既存の無料版機能（一覧/検索/詳細/比較/認証/取引損益/ウォッチリスト/価格更新）のみ。

**値は捏造していない。** 各項目は「今回のセッションで実際に実行したコマンドの結果」「コード自体を読んで確認した事実」「README.md/COORDINATION.mdに記載済みの、過去のセッションで確認されたと記録されている事実（本セッションでの再確認ではない）」のいずれかであり、出典を明記する。**このセッションおよびCodexは、本番のSupabase/Vercel環境に一切直接アクセスしていない**（方針上の制約）。「実環境未検証」は文字通り「本番で直接確認された記録が無い」ことを意味し、「壊れている」ことを意味しない。

---

## 1. 機能別完了判定

| 機能 | 実装済み | ローカル検証済み（本セッション） | 実環境検証 |
|---|---|---|---|
| 相場一覧（検索・弾フィルター・並び替え・ページング） | ✅ `src/app/page.tsx`, `MarketTable.tsx` | ✅ `npm run build`成功、コードレビュー | ⚠️ **未確認**（本番URLで3,270件表示されたとREADME.md記載＝2026-09-11時点の過去セッションでの確認。本セッションでの再確認なし） |
| カード詳細（チャート・AI判定・データ品質表示） | ✅ `src/app/cards/[id]/page.tsx`, `PriceChart.tsx` | ✅ build成功、`verify_price_stats.mjs`（15件PASS）でチャート裏の計算ロジックを検証 | ⚠️ 未確認（本セッション） |
| 比較（最大5枚、価格推移グラフ） | ✅ `CompareClient.tsx` | ✅ 本セッションでエラー処理・再試行導線を新規に発見・修正・検証（下記「3. 本セッションで発見・修正した不具合・改善」参照） | ⚠️ 未確認（本番Supabaseに対する実行はしていない。下記「2. 検証手法の内訳」参照） |
| 認証（メールのマジックリンク） | ✅ `src/app/login/page.tsx`, Supabase Auth | ✅ build成功、コードレビュー（メールtrim処理を本セッションで追加） | ⚠️ 未確認（本セッション。README.mdでは「本番URLでログインできるか確認」がユーザー自身の未完了タスクとして記載） |
| 取引損益（取引台帳・FIFO損益計算） | ✅ `PortfolioClient.tsx`, `src/lib/pnl.ts` | ✅ `verify_pnl_logic.mjs`（7シナリオ・18アサーションPASS）、本セッションで例外処理を追加・検証 | ⚠️ 未確認（本セッション。README.mdでは「取引を1件記録し含み損益が計算されるか」がユーザー自身の未完了タスクとして記載） |
| ウォッチリスト（価格条件アラート） | ✅ `WatchlistClient.tsx`, `check-watchlist`cron | ✅ `verify_check_watchlist.mjs`（36件PASS）、本セッションで例外処理を追加・検証 | ⚠️ 未確認（メール送信自体はPhase 4未着手のため対象外——条件判定・画面表示のみが対象） |
| 価格自動更新（onepiece-card-atari.jp、844件・`data_quality:'real'`） | ✅ `refresh-prices`cron | ✅ `verify_cron_time_budget.mjs`（15件PASS）でモック検証 | ⚠️ **Vercel Cronが実際に本番で自動実行されたかは、README.md記載時点（2026-09-11）で「単に未確認」——本セッションでも確認していない** |
| 価格自動更新（yuyu-tei.jp、2,426件・`data_quality:'partial'`） | ✅ コード実装・テスト完了（`refresh-yuyutei-prices`cron） | ✅ `verify_refresh_yuyutei_prices.mjs`（110件PASS） | ❌ **未デプロイ**（`git push`未実施、Supabaseに`yuyutei_sync_runs`/`app_settings`テーブル未作成——後述「4. 残件（本番反映に必要な作業）」参照） |

---

## 2. 検証手法の内訳（何を「ローカル検証済み」と呼んでいるか）

- **静的検証：** `npx tsc --noEmit`（型検査）・`npx eslint src --quiet`（lint）・`npm run build`（本番ビルド）を、本セッション中の全ての変更後に実行し、いずれも成功を確認している。
- **ロジック回帰テスト：** `migration/verify_*.mjs`（純粋関数・cronルートの実体をNode上でモック実行するテスト）全11ファイル、本ドキュメント作成時点で以下の通り全PASS：

  | ファイル | 結果 |
  |---|---|
  | verify_admin_auth.mjs | 全PASS（14アサーション） |
  | verify_app_settings.mjs | 全PASS（27アサーション） |
  | verify_check_watchlist.mjs | 36 passed, 0 failed |
  | verify_cron_time_budget.mjs | 15 passed, 0 failed |
  | verify_data_quality.mjs | 全PASS |
  | verify_form_validation.mjs | 全PASS（13アサーション） |
  | verify_format.mjs | 全PASS（9アサーション） |
  | verify_pnl_logic.mjs | 全PASS |
  | verify_price_stats.mjs | 15 passed, 0 failed |
  | verify_refresh_yuyutei_prices.mjs | 110 passed, 0 failed |
  | verify_yuyutei_parser.mjs | 全PASS（17アサーション） |

  いずれもDB接続・実ネットワーク通信なし（Supabaseクライアント・fetch・Date.now()等をモック）。Codex側でも本ドキュメント作成時点までに同じ11ファイルを独立に再実行し、全てexit 0を確認済みとの報告を受けている（Codexの作業ファイル`work/release-regression-results.json`——このセッションの作業ディレクトリには存在しないCodex側の記録）。

- **CompareClient.tsxの追加検証（2026-09-13、本セッションとCodexの相互検証）：** Codexが実際の`CompareClient.tsx`をTypeScriptでトランスパイルし、React hooks（`useState`/`useEffect`）とSupabaseクライアントだけを最小限モックした単体実行スクリプト（`review-compare.cjs`、本プロジェクトのディレクトリ外・Codex側の作業ディレクトリに配置）を作成。成功／例外／選択解除でのエラー消去／1000件超の2ページ目失敗時に部分キャッシュを残さないこと／実際の「再試行」ボタンのonClickハンドラ経由での再取得／選択解除後に古いリクエストが遅れて完了しても状態に反映されないこと、の6シナリオ全てPASS。**本セッションでも同じファイルを実際に実行し、同一の6件PASSを独立に確認済み。** ただし**これはブラウザE2Eテストではない**——実DOM・実React reconciler・実ブラウザは一切介さない、hand-rolledなReact hooks/Supabaseモックの上でコンポーネント関数を直接呼び出す形の検証であることを明記する（Codexからの明示的な依頼事項）。

- **本番環境への検証は一切行っていない：** 本セッション・Codexとも、本番のSupabase/Vercel環境に対する直接のクエリ・ログイン・デプロイ確認は行っていない（標準の制約として一貫して遵守）。README.md/COORDINATION.mdに記載のある「本番で確認済み」という表現は、全て**過去のセッション**（本セッションより前）で行われたと記録されている内容であり、本セッションでの再確認ではない。

---

## 3. 本セッションで発見・修正した不具合・改善（すべてローカルコミットのみ、push・本番操作は一切なし）

コミット数：`git status`で確認した時点で、ローカルの`main`ブランチが`origin/main`（GitHub上のリモート）に対して**73件**先行している（＝未push）。**【2026-09-13追記・Codexの指摘を受けて明記】これはローカルとGitHubリモートとのgitレベルの差分であり、Vercel上の本番デプロイが実際に今どのコミットを動かしているかを直接確認した結果ではない。** 通常Vercelは`origin/main`へのpushをトリガーに再デプロイする構成のはずだが、その設定自体・現在デプロイ中の実際のコミットハッシュを本セッションで確認してはいないため、「本番に73件分の差がある」と断定はできない——確度の高い推測に留まる。主な内容（詳細はCOORDINATION.mdの各セクション参照）：

**セキュリティ関連：**
- `/admin/sync-status`がログイン済みなら誰でも閲覧できた（管理者限定チェック欠如）→ページ・DB RLS両方に管理者限定ゲートを追加
- カード詳細ページのJSON-LDへの未エスケープ`<`によるstored XSS経路の可能性→エスケープ処理を追加
- 管理者判定（アプリ側trim+大小文字無視）とDB側RLS（完全一致）の正規化仕様の食い違い（Codex独立レビューで発見）→両方を同じ仕様に統一

**データ品質関連：**
- `backfill_source_urls.mjs`：yuyu-tei由来カードのURLを誤って上書きしうる欠陥
- `expand_catalog.mjs`：既に本体で修正済みのavg30/90計算バグを、独自コピーのせいで再導入しうる状態だった
- `computeStats()`：Postgres numeric列が文字列で返る場合の防御的変換漏れ

**堅牢性・UX改善：**
- 緊急停止スイッチのDB読み取りタイミングの是正
- 取引記録の枚数の整数検証漏れ
- 日時表示のタイムゾーン統一（JST固定、ハイドレーション不一致の解消）
- Portfolio/Watchlist/CompareClientの例外処理漏れ（busy状態固まり・無言失敗）の解消
- CompareClientのエラー取り残し・再試行導線の欠如（Codex独立レビューで発見、本ドキュメント作成直前に修正・双方で検証済み）
- `.env.local.example`が一度もコミットされていなかった問題

**未修正・議論継続中の論点（1件）：**
- 価格記録日（`price_snapshots.snapshot_date`）がUTC基準になっている構造的特性——Codexと合意の上、今は変更せず、将来移行する場合の互換条件のみCOORDINATION.mdに記載済み。挙動に実害はない（avg30/90計算は内部的に自己矛盾しない）と判断しているが、未解決の設計論点として記録。

---

## 4. 残件（本番反映に必要な作業——ユーザー自身の対応が必要）

このセッションは方針上、`git push`・本番DB照会/変更・デプロイを一切行っていない。無料版を実際に「完成」させるには、以下がユーザー自身の対応として残っている：

1. **`git push`する。** ローカルの`main`が`origin/main`に対して73件先行しており（本セッション分含む）、pushしない限りVercelの通常のデプロイフロー上は反映されないはずだが、実際に本番が今どのコミットで動いているかは本セッションで確認していない（上記「2. 検証手法の内訳」参照）。
2. **Vercelの環境変数に`ADMIN_EMAIL`を追加**してから再デプロイする（`/admin/sync-status`の管理者限定ゲートに必要）。
3. **Supabase SQL Editorで`migration/retrofit_admin_only_sync_runs.sql`を実行**する（実行前にファイル内のメールアドレスプレースホルダーを実際の管理者アドレスに置き換える）。
4. **（任意）遊々亭の日次自動追跡を有効化する場合：** `supabase/schema.sql`の`yuyutei_sync_runs`/`app_settings`テーブル定義をSupabase SQL Editorで実行（法務リスクはユーザー本人が既に受容済み——README.md「遊々亭ソースの法務リスクについて」参照）。
5. **本番でのVercel Cron自動実行の確認。** README.md記載時点（2026-09-11）で「実際に自動実行されたかは単に未確認」とされたままであり、本セッションでも確認していない。pushしてしばらく経ってから`/admin/sync-status`で実行履歴を確認する必要がある。
6. **本番URLでの一連の動作確認**（ログイン・ポートフォリオへの取引記録・含み損益計算）——README.mdの「起きたらまずやること」に記載されたまま、本セッション時点でもユーザー自身による確認は完了していない。

---

## 5. 残件（ユーザー入力が必要な情報）

- **`/privacy`（プライバシーポリシー）・`/terms`（利用規約）の事業者情報プレースホルダー：**
  - `/privacy`：事業者名・屋号（`src/app/privacy/page.tsx:23`）、所在地（同26行目）、連絡先（同28・106行目）が`［ここに記入］`のプレースホルダーのまま。
  - `/terms`：事業者名・屋号（`src/app/terms/page.tsx:23`）、管轄裁判所（同92行目）、連絡先（同99行目）、最終更新日（同12行目）が同様にプレースホルダーのまま。
  - 両ページとも`robots: { index: false }`（noindex）が既に設定されており、フッター等からのリンクも意図的に未追加。**【2026-09-13追記・Codexの指摘を受けて訂正】これはアクセス制御ではない。** noindexは検索エンジンへの掲載を抑制するだけ、フッターリンク未追加はサイト内の導線が無いだけであり、**URLを直接入力すれば誰でも`/privacy`・`/terms`をプレースホルダーのまま閲覧できる**（ログイン等の認証も課されていない）。「一般公開されないようにする安全策」ではなく、「検索インデックスからの露出と、サイト内からの発見導線を抑えているだけ」という程度の対策であり、直リンクやSNS共有等で第三者がプレースホルダーのまま閲覧すること自体は防げていない。ユーザー本人の情報を入力後、`noindex`を外し`Footer.tsx`にリンクを追加することが推奨される（README.md記載）。
  - 有料プラン（Stripe）開始時には解約・返金条項の追加も別途必要（現状は無料機能のみを前提とした内容）。

---

## 6. 明示的にスコープ外（この「完成」判定に含めない）

Codexの指示により、以下は今回の完成条件に含めない（ユーザーから依頼されていない新機能のため）：

- Phase 3: Stripe決済（未着手、Stripeアカウント作成が前提）
- Phase 4: メール通知の実送信（条件判定ロジックは実装・テスト済みだが、Resendアカウント作成とメール送信部分は未着手）
- AI市場サマリー機能（有料Claude APIが必要、ユーザー判断待ちのまま）
- アフィリエイト収益化（ASPアカウント開設が前提、未着手）
- 多言語・多ゲーム展開（時期尚早と判断済み、README.md記載）

---

## まとめ

**無料版として依頼されている機能（一覧/検索/詳細/比較/認証/取引損益/ウォッチリスト/価格更新）は、いずれも実装済みであり、本セッションで可能な範囲のローカル検証（型検査・lint・本番相当ビルド・11ファイルの回帰テスト・CompareClientの実行時検証）は完了している。** 一方で、(a) ローカルの`main`が`origin/main`に対して73件先行しており未push（本番デプロイの実際の状態そのものは本セッションで未確認）、(b) 本番環境でのCronの自動実行・ログイン等の一連の動作は、本セッションはもとより直近のREADME.md更新時点でも未確認のまま、(c) `/privacy`・`/terms`の事業者情報が未入力でアクセス制限も無い（URL直打ちで誰でも閲覧可能）、という3種類の残件があり、これらはコード修正では完結せず、ユーザー自身の対応（push、Vercel/Supabaseでの設定、事業者情報の入力）を要する。
